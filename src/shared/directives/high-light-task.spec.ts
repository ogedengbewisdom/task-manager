import { HighLightTask } from './high-light-task';

describe('HighLightTask', () => {
  it('should create an instance', () => {
    const directive = new HighLightTask();
    expect(directive).toBeTruthy();
  });
});
